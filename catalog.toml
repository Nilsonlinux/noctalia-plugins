# This file is auto-generated. Do not edit manually.
# Do not include it in your commit.
# Noctalia plugins catalog.
# Index of every plugin this source ships: the minimum needed to render, search,
# and compat-check the list. The per-plugin plugin.toml stays authoritative; the
# host re-reads it on enable. Keep one [[plugin]] row per plugin subdirectory.
# A [[plugin.release]] row names an older revision for a Noctalia below the tip's
# plugin_api, so an older release stays installable instead of the plugin vanishing.
# On every row, updated_at dates the commit that first shipped that row's version.

[[plugin]]
id = "nilsonlinux/link-ip-monitor"
name = "Link/IP Monitor"
version = "1.0.1"
updated_at = 1788127260
added_at = 1788127260
author = "Nilsonlinux"
license = "MIT"
icon = "activity"
description = "It pings a list of IPs, hosts, or links at intervals and notifies you when one goes down (or comes back up)."
plugin_api = 14
tags = ["network", "indicator", "bar", "panel", "service"]

[[plugin]]
id = "nilsonlinux/rss-notifier"
name = "RSS/Atom Notifier"
version = "1.0.3"
updated_at = 1788127260
added_at = 1788127260
author = "Nilsonlinux"
license = "MIT"
icon = "rss"
description = "Monitors RSS/Atom feeds and notifies you when new items appear."
plugin_api = 14
tags = ["utility", "indicator", "panel", "bar", "service"]

[[plugin]]
id = "nilsonlinux/inventory-manager"
name = "Inventory Manager"
version = "1.0.1"
updated_at = 1788559302
added_at = 1788127260
author = "Nilsonlinux"
license = "MIT"
icon = "package"
description = "Inventory Manager for managing product inventory with categories, low stock alerts, and data export."
plugin_api = 15
tags = ["utility", "productivity", "service", "panel", "bar"]

[[plugin.release]]
plugin_api = 14
version = "1.0.1"
updated_at = 1788559302
rev = "e57c7091bca5a227c86bfd79335d4bc044d96489"
